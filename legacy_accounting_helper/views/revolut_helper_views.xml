<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Search ───────────────────────────────────────────────────────────── -->
    <record id="view_revolut_transaction_search" model="ir.ui.view">
        <field name="name">revolut.transaction.search</field>
        <field name="model">revolut.transaction</field>
        <field name="arch" type="xml">
            <search string="Search Transactions">
                <field name="description" string="Description"/>
                <field name="merchant_name" string="Merchant"/>
                <field name="reference" string="Reference"/>
                <field name="revolut_id" string="Revolut ID"/>
                <field name="revolut_tx_ref" string="TX Ref"/>
                <field name="account_name" string="Account"/>
                <field name="transfer_other_account_id" string="Other Account ID"/>
                <field name="currency" string="Currency"/>

                <!-- State filters -->
                <filter name="state_completed" string="Completed"
                        domain="[('state','=','completed')]"/>
                <filter name="state_pending" string="Pending"
                        domain="[('state','=','pending')]"/>
                <filter name="state_declined" string="Declined / Failed"
                        domain="[('state','in',['declined','failed','reverted'])]"/>

                <separator/>

                <!-- Injection filters -->
                <filter name="filter_injected" string="Injected to Accounting"
                        domain="[('is_injected','=',True)]"/>
                <filter name="filter_not_injected" string="Not Injected"
                        domain="[('is_injected','=',False)]"/>

                <separator/>

                <!-- Accounting pipeline stage filters -->
                <filter name="stage_needs_bill" string="Needs bill/invoice"
                        domain="[('accounting_stage','=','needs_bill')]"/>
                <filter name="stage_bill_draft" string="Bill/invoice draft"
                        domain="[('accounting_stage','=','bill_draft')]"/>
                <filter name="stage_to_reconcile" string="To reconcile"
                        domain="[('accounting_stage','=','to_reconcile')]"/>
                <filter name="stage_reconciled" string="Reconciled (done)"
                        domain="[('accounting_stage','=','reconciled')]"/>
                <separator/>

                <!-- Vendor bill filters -->
                <filter name="filter_has_bill" string="Has Vendor Bill"
                        domain="[('vendor_bill_id','!=',False)]"/>
                <filter name="filter_no_bill" string="No Vendor Bill"
                        domain="[('vendor_bill_id','=',False)]"/>
                <filter name="filter_reconciled" string="Reconciled"
                        domain="[('is_bill_reconciled','=',True)]"/>
                <filter name="filter_not_reconciled" string="Not Reconciled"
                        domain="[('is_bill_reconciled','=',False)]"/>

                <separator/>

                <!-- Type filters -->
                <filter name="type_card" string="Card Payments"
                        domain="[('transaction_type','in',['card_payment','card_refund','card_chargeback','card_credit'])]"/>
                <filter name="type_transfer" string="Transfers"
                        domain="[('transaction_type','=','transfer')]"/>
                <filter name="type_exchange" string="Exchanges"
                        domain="[('transaction_type','=','exchange')]"/>
                <filter name="type_topup" string="Top-ups"
                        domain="[('transaction_type','in',['topup','topup_return'])]"/>
                <filter name="type_fee" string="Fees / Tax"
                        domain="[('transaction_type','in',['fee','tax','tax_refund'])]"/>
                <filter name="type_internal_transfer" string="Internal Transfers"
                        domain="[('transfer_between_accounts','=',True)]"/>
                <filter name="type_fcf" string="FCF (Cash Funds)"
                        domain="[('transaction_type','in',['fcf_buy','fcf_sell','fcf_interest','fcf_fee'])]"/>
                <filter name="filter_synthetic" string="Synthetic (Fees)"
                        domain="[('is_synthetic','=',True)]"/>

                <separator/>

                <!-- Source filters -->
                <filter name="source_api" string="API Transactions"
                        domain="[('source','=','api')]"/>
                <filter name="source_csv" string="CSV Imported"
                        domain="[('source','=','csv')]"/>

                <separator/>

                <filter name="filter_date_local" string="Settlement Date (Local)" date="settlement_date_local"/>
                <filter name="filter_date_utc" string="Settlement Date (UTC)" date="settlement_date"/>

                <separator/>

                <!-- Receipt filters -->
                <filter name="filter_has_receipt" string="Has Receipt"
                        domain="[('has_receipt','=',True)]"/>
                <filter name="filter_no_receipt" string="No Receipt"
                        domain="[('has_receipt','=',False)]"/>
                <filter name="filter_document_lost" string="Document Lost"
                        domain="[('document_status','=','lost')]"/>
                <filter name="filter_document_later" string="Might Find Later"
                        domain="[('document_status','=','later')]"/>
                <filter name="filter_manual_search_needed" string="Invoice Manual Search Needed"
                        domain="[('manual_invoice_search_needed','=',True)]"/>

                <group expand="0" string="Group By">
                    <filter name="group_stage"    string="Accounting Stage" context="{'group_by': 'accounting_stage'}"/>
                    <filter name="group_type"     string="Type"            context="{'group_by': 'transaction_type'}"/>
                    <filter name="group_state"    string="State"           context="{'group_by': 'state'}"/>
                    <filter name="group_currency" string="Currency"        context="{'group_by': 'currency'}"/>
                    <filter name="group_account"  string="Account"         context="{'group_by': 'account_name'}"/>
                    <filter name="group_other_account" string="Other Account ID" context="{'group_by': 'transfer_other_account_id'}"/>
                    <filter name="group_source"   string="Source"          context="{'group_by': 'source'}"/>
                    <filter name="group_country"  string="Merchant Country" context="{'group_by': 'merchant_country'}"/>
                    <filter name="group_has_receipt" string="Has Receipt"  context="{'group_by': 'has_receipt'}"/>
                    <filter name="group_reconciled" string="Reconciled"  context="{'group_by': 'is_bill_reconciled'}"/>
                    <filter name="group_document_status" string="Doc Status"  context="{'group_by': 'document_status'}"/>
                    <separator/>
                    <filter name="group_month_local" string="Month (Local)" context="{'group_by': 'settlement_date_local:month'}"/>
                    <filter name="group_year_local"  string="Year (Local)"  context="{'group_by': 'settlement_date_local:year'}"/>
                    <separator/>
                    <filter name="group_month_utc" string="Month (UTC)"    context="{'group_by': 'settlement_date:month'}"/>
                    <filter name="group_year_utc"  string="Year (UTC)"     context="{'group_by': 'settlement_date:year'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ── Tree ─────────────────────────────────────────────────────────────── -->
    <record id="view_revolut_transaction_tree" model="ir.ui.view">
        <field name="name">revolut.transaction.tree</field>
        <field name="model">revolut.transaction</field>
        <field name="arch" type="xml">
            <tree string="Revolut Transactions" default_order="created_at desc"
                  decoration-success="state == 'completed'"
                  decoration-warning="state in ('pending', 'created')"
                  decoration-danger="state in ('declined', 'failed', 'reverted')">
                <header>
                    <button name="action_sync_all_from_revolut"
                            string="Sync All from Revolut"
                            type="object"
                            class="btn-primary"
                            display="always"
                            icon="fa-refresh"/>
                    <button name="action_reindex_accounts"
                            string="Reindex Accounts"
                            type="object"
                            class="btn-secondary"
                            display="always"
                            icon="fa-link"/>
                </header>
                <field name="revolut_id"            string="TX ID"               optional="hide"/>
                <field name="revolut_tx_ref"        string="TX Ref"              optional="hide"/>
                <field name="settlement_date_local" string="Settlement Date (Local)" optional="show"/>
                <field name="settlement_date"   string="Settlement Date (UTC)" optional="hide"/>
                <field name="settlement_datetime_utc" string="Settlement Timestamp (UTC)" optional="hide"/>
                <field name="transaction_type"  string="Type"        optional="show"/>
                <field name="state"             string="State"       widget="badge"
                       decoration-success="state == 'completed'"
                       decoration-warning="state in ('pending', 'created')"
                       decoration-danger="state in ('declined', 'failed', 'reverted')"
                       optional="show"/>
                <field name="source"            string="Source"      widget="badge"
                       decoration-info="source == 'csv'"
                       decoration-success="source == 'api'"
                       optional="show"/>
                <field name="is_synthetic"      string="Synthetic"   widget="boolean"
                       optional="hide" readonly="1"/>
                <field name="description"       string="Description" optional="show"/>
                <field name="merchant_name"     string="Merchant"    optional="show"/>
                <field name="merchant_country"  string="Country"     optional="hide"/>
                <field name="amount"            string="Amount"      optional="show"/>
                <field name="tx_fee"            string="Fee"         optional="hide"/>
                <field name="currency"          string="Currency"    optional="show"/>
                <field name="bill_amount"       string="Original Amount"   optional="hide"/>
                <field name="bill_currency"     string="Original Currency" optional="hide"/>
                <field name="balance_after"     string="Balance"     optional="hide"/>
                <field name="card_number"       string="Card Number" optional="hide"/>
                <field name="card_first_name"   string="First Name"  optional="hide"/>
                <field name="card_last_name"    string="Last Name"   optional="hide"/>
                <field name="account_name"      string="Account"     optional="show"/>
                <field name="account_revolut_id" string="Account ID"  optional="hide"/>
                <field name="transfer_between_accounts" string="Int. Transfer"
                       widget="boolean" optional="hide" readonly="1"/>
                <field name="transfer_other_account_id" string="Other Account ID"
                       optional="hide" readonly="1"/>
                <field name="reference"                 string="Reference"   optional="hide"/>
                <field name="reason_code"               string="Reason"      optional="hide"/>
                <field name="document_status" string="Doc Status"
                       optional="hide" readonly="1"
                       decoration-danger="document_status == 'lost'"
                       decoration-warning="document_status == 'later'"/>
                <field name="invoice_attachment_count"  string="Receipts"    optional="hide"
                       decoration-success="invoice_attachment_count &gt; 0"
                       decoration-muted="invoice_attachment_count == 0"/>
                <field name="receipt_list_html"  string="Receipt"     optional="show"
                       widget="html" readonly="1"/>
                <field name="accounting_stage" string="Stage" widget="badge" optional="show"
                       decoration-info="accounting_stage in ('to_inject','injected')"
                       decoration-warning="accounting_stage in ('needs_bill','bill_draft','to_reconcile')"
                       decoration-success="accounting_stage == 'reconciled'"/>
                <field name="is_injected" string="Accounting" widget="boolean"
                       optional="hide" readonly="1"
                       decoration-success="is_injected"
                       decoration-muted="not is_injected"/>
                <field name="has_vendor_bill" string="Bill" widget="boolean"
                       optional="hide" readonly="1"
                       decoration-success="has_vendor_bill"
                       decoration-muted="not has_vendor_bill"/>
                <field name="is_bill_reconciled" string="Reconciled" widget="boolean"
                       optional="hide" readonly="1"
                       decoration-success="is_bill_reconciled"
                       decoration-muted="not is_bill_reconciled"/>
                <field name="manual_invoice_search_needed" string="Manual Search"
                       widget="inline_boolean_toggle" optional="hide"/>
                <field name="routed_account_id" string="GL Category" optional="show" readonly="1"/>
                <field name="matched_rule_id" string="Matched Rule" optional="show" readonly="1"/>
            </tree>
        </field>
    </record>

    <!-- ── Form ─────────────────────────────────────────────────────────────── -->
    <record id="view_revolut_transaction_form" model="ir.ui.view">
        <field name="name">revolut.transaction.form</field>
        <field name="model">revolut.transaction</field>
        <field name="arch" type="xml">
            <form string="Revolut Transaction" create="false">
                <header>
                    <field name="is_injected" invisible="1"/>
                    <field name="has_vendor_bill" invisible="1"/>
                    <field name="has_customer_invoice" invisible="1"/>
                    <field name="invoice_attachment_count" invisible="1"/>
                    <field name="is_bill_reconciled" invisible="1"/>
                    <!-- 1. Bank tx -->
                    <button name="action_inject_to_accounting"
                            string="1. Inject Bank Tx"
                            type="object"
                            class="btn-primary"
                            icon="fa-arrow-right"
                            invisible="is_injected or state != 'completed'"/>
                    <button name="action_remove_from_accounting"
                            string="Remove Bank Tx"
                            type="object"
                            class="btn-secondary"
                            icon="fa-undo"
                            invisible="not is_injected"/>
                    <!-- 2. Bill from receipt -->
                    <button name="action_create_vendor_bill"
                            string="2. Inject attached Bill"
                            type="object"
                            class="btn-primary"
                            icon="fa-file-text-o"
                            invisible="has_vendor_bill or invoice_attachment_count == 0"/>
                    <button name="action_remove_attached_bill"
                            string="Remove attached Bill"
                            type="object"
                            class="btn-secondary"
                            icon="fa-trash-o"
                            invisible="not has_vendor_bill"/>
                    <!-- 3. Reconcile -->
                    <button name="action_reconcile_vendor_bill"
                            string="3. Reconcile bill &amp; tx"
                            type="object"
                            class="btn-primary"
                            icon="fa-check-square-o"
                            invisible="not has_vendor_bill or not is_injected or is_bill_reconciled"/>
                    <button name="action_unreconcile_bill"
                            string="Unreconcile bill &amp; tx"
                            type="object"
                            class="btn-secondary"
                            icon="fa-chain-broken"
                            invisible="not has_vendor_bill or not is_bill_reconciled"/>
                    <button name="action_open_vendor_bill"
                            string="Open Bill"
                            type="object"
                            class="btn-link"
                            icon="fa-external-link"
                            invisible="not has_vendor_bill"/>
                    <button name="action_download_documents"
                            string="Download Documents"
                            type="object"
                            class="btn-link"
                            icon="fa-download"
                            invisible="invoice_attachment_count == 0 and not has_vendor_bill"/>
                    <!-- Full teardown -->
                    <button name="action_remove_all_from_accounting"
                            string="Remove from Accounting"
                            type="object"
                            class="btn-secondary"
                            icon="fa-eraser"
                            confirm="Remove EVERYTHING this tx put into Accounting — unreconcile, delete the injected bill, and delete the bank line. Pre-existing attached bills are kept (just unlinked). Continue?"
                            invisible="not is_injected and not has_vendor_bill"/>
                    <field name="accounting_stage" widget="statusbar"
                           statusbar_visible="to_inject,needs_bill,bill_draft,to_reconcile,reconciled"/>
                </header>
                <sheet>
                    <group>
                        <group string="Transaction">
                            <field name="revolut_id"        string="Revolut ID"/>
                            <field name="state" widget="badge"
                                   decoration-success="state == 'completed'"
                                   decoration-warning="state in ('pending', 'created')"
                                   decoration-danger="state in ('declined', 'failed', 'reverted')"/>
                            <field name="transaction_type"  string="Type"/>
                            <field name="amount"/>
                            <field name="currency"/>
                            <field name="bill_amount"   string="Original Amount"
                                   invisible="not bill_currency"/>
                            <field name="bill_currency" string="Original Currency"
                                   invisible="not bill_currency"/>
                            <field name="description"/>
                            <field name="reference"/>
                            <field name="reason_code"/>
                            <field name="related_transaction_id" string="Related Transaction"/>
                            <field name="statement_line_id" string="Statement Line"
                                   readonly="1" invisible="not is_injected"/>
                            <field name="routed_account_id" string="GL Category"
                                   readonly="1"
                                   help="GL account the matched Injection Rule routes this expense to. Set when you run 'Inject attached Bill'."/>
                            <field name="matched_rule_id" string="Matched Rule"
                                   readonly="1"
                                   help="Which Injection Rule matched this transaction. Set when you run 'Inject attached Bill'."/>
                            <field name="vendor_bill_id" string="Vendor Bill"
                                   readonly="1" invisible="not has_vendor_bill"/>
                        </group>
                        <group string="Timing">
                            <field name="created_at"/>
                            <field name="updated_at"/>
                            <field name="completed_at"/>
                            <field name="scheduled_for"/>
                        </group>
                    </group>
                    <group>
                        <group string="Account">
                            <field name="account_map_id" string="Account"
                                   invisible="not account_map_id"/>
                            <field name="account_name"
                                   invisible="account_map_id"/>
                            <field name="account_revolut_id" string="Account ID"/>
                            <field name="source" widget="badge"
                                   decoration-info="source == 'csv'"
                                   decoration-success="source == 'api'"/>
                            <field name="company_id"/>
                            <field name="transfer_between_accounts" invisible="1"/>
                            <field name="transfer_other_account_map_id"
                                   string="Transfer To/From"
                                   invisible="not transfer_between_accounts or not transfer_other_account_map_id"/>
                            <field name="transfer_other_account_name"
                                   string="Transfer To/From"
                                   invisible="not transfer_between_accounts or transfer_other_account_map_id"/>
                            <field name="transfer_other_account_id"
                                   string="Other Account ID"
                                   invisible="not transfer_between_accounts"/>
                        </group>
                        <group string="Merchant" invisible="not merchant_name">
                            <field name="merchant_name"/>
                            <field name="merchant_city"/>
                            <field name="merchant_country"/>
                            <field name="merchant_category_code" string="MCC"/>
                        </group>
                        <group string="Card" invisible="not card_number">
                            <field name="card_number"/>
                            <field name="card_first_name" string="First Name"/>
                            <field name="card_last_name"  string="Last Name"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Supporting Documents" name="supporting_docs">

                            <!-- Hidden fields required for invisible expressions -->
                            <field name="receipt_is_pdf" invisible="1"/>
                            <field name="google_user_connected" invisible="1"/>
                            <field name="gmail_search_performed" invisible="1"/>
                            <field name="gmail_search_results_count" invisible="1"/>
                            <field name="revolut_fetch_performed" invisible="1"/>
                            <field name="revolut_fetched_count" invisible="1"/>

                            <!-- ── Document Status ────────────────────────── -->
                            <div class="mb-3 d-flex align-items-center gap-2">
                                <button name="action_set_document_lost"
                                        string="Mark Lost"
                                        type="object"
                                        class="btn-danger btn-sm"
                                        icon="fa-times-circle"
                                        invisible="document_status == 'lost'"/>
                                <button name="action_set_document_later"
                                        string="Might Find Later"
                                        type="object"
                                        class="btn-warning btn-sm"
                                        icon="fa-clock-o"
                                        invisible="document_status == 'later'"/>
                                <button name="action_set_document_ok"
                                        string="Clear Status"
                                        type="object"
                                        class="btn-secondary btn-sm"
                                        icon="fa-undo"
                                        invisible="document_status == 'ok'"/>
                                <field name="document_status" invisible="1"/>
                                <span class="ms-2 fw-bold text-danger"
                                      invisible="document_status != 'lost'">
                                    Supporting document is lost
                                </span>
                                <span class="ms-2 fw-bold text-warning"
                                      invisible="document_status != 'later'">
                                    Might find later
                                </span>
                            </div>

                            <!-- ── Attached Documents ─────────────────────── -->
                            <div class="o_horizontal_separator mb-2">Attached Documents</div>

                            <!-- Receipt preview -->
                            <div class="text-center mb-3" invisible="not receipt_preview">
                                <field name="receipt_preview"
                                       widget="image"
                                       options="{'size': [400, 600]}"
                                       readonly="1"
                                       nolabel="1"
                                       style="max-width:400px; max-height:600px; object-fit:contain; border:1px solid #dee2e6; border-radius:4px;"/>
                                <div class="text-muted small mt-1">
                                    <field name="receipt_preview_filename" readonly="1" nolabel="1"/>
                                </div>
                            </div>
                            <div class="mb-3" invisible="not receipt_is_pdf">
                                <button name="action_open_receipt_pdf"
                                        string="Open PDF Receipt"
                                        type="object"
                                        class="btn-secondary"
                                        icon="fa-file-pdf-o"/>
                            </div>

                            <field name="invoice_attachment_ids"
                                   widget="many2many_binary"
                                   nolabel="1"
                                   string="Attached Invoice/Bill"/>

                            <!-- ── Revolut Receipts ───────────────────────── -->
                            <div class="o_horizontal_separator mt-4 mb-2 d-flex align-items-center gap-2">
                                <img src="/legacy_accounting_helper/static/src/img/Revolu_icon.png"
                                     style="height:20px;width:20px;object-fit:contain;"/>
                                Revolut Receipts
                            </div>
                            <div class="mb-2 d-flex gap-2">
                                <button name="action_fetch_revolut_staged"
                                        string="Fetch from Revolut"
                                        type="object"
                                        class="btn-primary"
                                        icon="fa-cloud-download"/>
                            </div>

                            <div class="alert alert-info mb-2"
                                 invisible="not revolut_fetch_performed or revolut_fetched_count != 0">
                                No receipts found in Revolut for this transaction.
                            </div>

                            <field name="revolut_fetched_receipt_ids"
                                   nolabel="1"
                                   invisible="revolut_fetched_count == 0">
                                <tree create="false" delete="false" editable="false">
                                    <field name="name" string="Filename"/>
                                    <field name="mime_type" string="Type" optional="hide"/>
                                    <field name="is_attached" widget="boolean"
                                           string="Attached" readonly="1"/>
                                    <button name="action_preview"
                                            string="Preview"
                                            type="object"
                                            icon="fa-eye"
                                            class="btn-secondary btn-sm"/>
                                    <button name="action_attach"
                                            string="Attach"
                                            type="object"
                                            icon="fa-paperclip"
                                            class="btn-primary btn-sm"
                                            invisible="is_attached"/>
                                    <button name="action_attach"
                                            string="Re-attach"
                                            type="object"
                                            icon="fa-refresh"
                                            class="btn-secondary btn-sm"
                                            invisible="not is_attached"/>
                                </tree>
                            </field>

                            <!-- ── Gmail Lookup ───────────────────────────── -->
                            <div class="o_horizontal_separator mt-4 mb-2 d-flex align-items-center gap-2">
                                <img src="/legacy_accounting_helper/static/src/img/Gmail_icon.png"
                                     style="height:20px;width:20px;object-fit:contain;"/>
                                Gmail Lookup
                            </div>

                            <!-- Not connected warning -->
                            <div class="alert alert-warning mt-2"
                                 invisible="google_user_connected">
                                Connect your Google account first:
                                <i>Revolut Business API Integration → Google / Gmail Setup</i>
                            </div>

                            <!-- Search inputs (when connected) -->
                            <group invisible="not google_user_connected">
                                <group>
                                    <field name="gmail_search_keywords"
                                           placeholder="invoice payment Slack…"/>
                                    <field name="gmail_search_date"/>
                                    <field name="gmail_search_range"/>
                                </group>
                                <group>
                                    <field name="gmail_search_with_attachment"
                                           widget="boolean_toggle"/>
                                </group>
                            </group>
                            <div class="mb-3 d-flex gap-2"
                                 invisible="not google_user_connected">
                                <button name="action_gmail_search"
                                        string="Search Gmail"
                                        type="object"
                                        class="btn-primary"
                                        icon="fa-search"/>
                                <button name="action_gmail_clear"
                                        string="Clear"
                                        type="object"
                                        class="btn-secondary"
                                        icon="fa-times"
                                        invisible="not gmail_search_performed"/>
                                <button name="action_ai_analyze"
                                        string="AI Analyze"
                                        type="object"
                                        class="btn-secondary"
                                        icon="fa-magic"
                                        invisible="gmail_search_results_count == 0"/>
                            </div>

                            <!-- Flag: Gmail didn't surface it — needs a manual hunt -->
                            <div class="mb-3 d-flex align-items-center gap-2">
                                <field name="manual_invoice_search_needed"
                                       widget="boolean_toggle" nolabel="1"/>
                                <span>Invoice Manual Search Needed
                                    <i class="text-muted">— tried Gmail; follow the email's links or look outside Gmail.</i>
                                </span>
                            </div>

                            <!-- Found messages tree (with View Email button) -->
                            <div class="o_horizontal_separator mt-3 mb-2"
                                 invisible="not gmail_search_performed">
                                Emails Found
                            </div>
                            <field name="gmail_message_ids"
                                   nolabel="1"
                                   invisible="not gmail_search_performed">
                                <tree create="false" delete="false" editable="false">
                                    <field name="sender" string="From"/>
                                    <field name="subject" string="Subject"/>
                                    <field name="email_date" string="Date"/>
                                    <field name="attachment_count" string="Attachments"/>
                                    <field name="snippet" string="Snippet" optional="hide"/>
                                    <button name="action_view_email"
                                            string="View Email"
                                            type="object"
                                            icon="fa-envelope-open-o"
                                            class="btn-primary btn-sm"/>
                                </tree>
                            </field>

                            <!-- Found attachments tree -->
                            <div class="o_horizontal_separator mt-3 mb-2"
                                 invisible="gmail_search_results_count == 0">
                                Attachments Found
                            </div>
                            <field name="gmail_found_attachment_ids"
                                   nolabel="1"
                                   invisible="gmail_search_results_count == 0">
                                <tree create="false" delete="false" editable="false"
                                      decoration-success="is_ai_selected">
                                    <field name="is_ai_selected" string="AI" widget="boolean" readonly="1"/>
                                    <field name="ai_selection_reason" string="AI Reason" optional="show" readonly="1"/>
                                    <field name="email_from" string="From"/>
                                    <field name="email_subject" string="Subject"/>
                                    <field name="email_date" string="Email Date"/>
                                    <field name="name" string="Filename"/>
                                    <field name="mime_type" string="Type" optional="hide"/>
                                    <field name="size_display" string="Size"/>
                                    <field name="is_attached" widget="boolean"
                                           string="Attached" readonly="1"/>
                                    <button name="action_preview"
                                            string="Preview"
                                            type="object"
                                            icon="fa-eye"
                                            class="btn-secondary btn-sm"/>
                                    <button name="action_attach"
                                            string="Attach"
                                            type="object"
                                            icon="fa-paperclip"
                                            class="btn-primary btn-sm"
                                            invisible="is_attached"/>
                                    <button name="action_attach"
                                            string="Re-attach"
                                            type="object"
                                            icon="fa-refresh"
                                            class="btn-secondary btn-sm"
                                            invisible="not is_attached"/>
                                </tree>
                            </field>

                            <!-- ── Vendor Bill (outgoing tx, or already linked) ── -->
                            <div invisible="not has_vendor_bill and amount &gt;= 0">
                                <div class="o_horizontal_separator mt-4 mb-2 d-flex align-items-center gap-2">
                                    <i class="fa fa-file-text-o"/>
                                    Vendor Bill
                                </div>

                                <!-- No bill yet → pick one manually and attach it. -->
                                <group invisible="has_vendor_bill" col="12">
                                    <field name="manual_bill_pick_id" colspan="9"
                                           options="{'no_create': True, 'no_open': True}"/>
                                    <button name="action_attach_manual_bill"
                                            string="Attach Bill"
                                            type="object"
                                            class="btn-primary"
                                            icon="fa-link"
                                            colspan="3"
                                            invisible="not manual_bill_pick_id"/>
                                </group>
                                <div class="text-muted small mb-2" invisible="has_vendor_bill">
                                    Type a vendor bill number (e.g. <strong>BILL/2026/02/0026</strong>) to
                                    link an existing unpaid bill to this transaction.
                                </div>

                                <!-- Bill linked → show it, preview it, allow removal. -->
                                <group invisible="not has_vendor_bill">
                                    <field name="vendor_bill_id" readonly="1"/>
                                    <field name="is_bill_reconciled" widget="boolean_toggle" readonly="1"/>
                                </group>
                                <div class="mb-2" invisible="not has_vendor_bill">
                                    <button name="action_unlink_vendor_bill"
                                            string="Remove Attached Bill"
                                            type="object"
                                            class="btn-secondary btn-sm"
                                            icon="fa-chain-broken"
                                            confirm="Remove this vendor bill from the transaction? It will also remove the bill's document from receipts and unreconcile it if reconciled. The bill itself is kept."/>
                                </div>
                                <field name="vendor_bill_preview_html" widget="html" nolabel="1"
                                       readonly="1" invisible="not has_vendor_bill"/>
                            </div>

                            <!-- ── Customer Invoice (incoming tx, or already linked) ── -->
                            <div invisible="not has_customer_invoice and amount &lt;= 0">
                                <div class="o_horizontal_separator mt-4 mb-2 d-flex align-items-center gap-2">
                                    <i class="fa fa-file-text-o"/>
                                    Customer Invoice
                                </div>

                                <!-- No invoice yet → pick one manually and attach it. -->
                                <group invisible="has_customer_invoice" col="12">
                                    <field name="manual_invoice_pick_id" colspan="9"
                                           options="{'no_create': True, 'no_open': True}"/>
                                    <button name="action_attach_manual_invoice"
                                            string="Attach Invoice"
                                            type="object"
                                            class="btn-primary"
                                            icon="fa-link"
                                            colspan="3"
                                            invisible="not manual_invoice_pick_id"/>
                                </group>
                                <div class="text-muted small mb-2" invisible="has_customer_invoice">
                                    Type a customer invoice number (e.g. <strong>INV/2026/00042</strong>) to
                                    link an existing unpaid invoice to this transaction, so it can be
                                    reconciled automatically.
                                </div>

                                <!-- Invoice linked → show it, preview it, allow removal. -->
                                <group invisible="not has_customer_invoice">
                                    <field name="customer_invoice_id" readonly="1"/>
                                    <field name="is_bill_reconciled" string="Reconciled"
                                           widget="boolean_toggle" readonly="1"/>
                                </group>
                                <div class="mb-2" invisible="not has_customer_invoice">
                                    <button name="action_unlink_customer_invoice"
                                            string="Remove Attached Invoice"
                                            type="object"
                                            class="btn-secondary btn-sm"
                                            icon="fa-chain-broken"
                                            confirm="Remove this customer invoice from the transaction? It will also remove the invoice's document from receipts and unreconcile it if reconciled. The invoice itself is kept."/>
                                </div>
                                <field name="customer_invoice_preview_html" widget="html" nolabel="1"
                                       readonly="1" invisible="not has_customer_invoice"/>
                            </div>
                        </page>
                        <page string="Legs" name="legs">
                            <field name="leg_ids" nolabel="1">
                                <tree string="Legs" create="false" edit="false">
                                    <field name="leg_id"                    string="Leg ID"/>
                                    <field name="amount"/>
                                    <field name="currency"/>
                                    <field name="fee"/>
                                    <field name="bill_amount"               optional="hide"/>
                                    <field name="bill_currency"             optional="hide"/>
                                    <field name="balance"                   string="Balance After"/>
                                    <field name="description"/>
                                    <field name="account_id"                string="Account ID"/>
                                    <field name="counterparty_id"           optional="hide"/>
                                    <field name="counterparty_account_type" optional="hide"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Raw JSON" name="raw_json">
                            <field name="raw_json" widget="text" nolabel="1" readonly="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ── Server action: batch fetch from Revolut (appears in ⚙ Actions menu) -->
    <record id="action_server_fetch_revolut_attachments" model="ir.actions.server">
        <field name="name">Fetch Attachments from Revolut</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_fetch_revolut_attachments()</field>
    </record>

    <!-- ── Server action: batch inject to accounting (appears in ⚙ Actions menu) -->
    <record id="action_server_inject_to_accounting" model="ir.actions.server">
        <field name="name">1. Inject Bank Txs</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_inject_to_accounting()</field>
    </record>

    <!-- ── Server action: batch remove bank txs (appears in ⚙ Actions menu) -->
    <record id="action_server_remove_from_accounting" model="ir.actions.server">
        <field name="name">Remove Bank Txs</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_remove_from_accounting()</field>
    </record>

    <!-- ── Server action: create a draft bill from the attached receipt -->
    <record id="action_server_create_vendor_bills" model="ir.actions.server">
        <field name="name">2. Inject attached Bill</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_create_vendor_bill()</field>
    </record>

    <!-- ── Server action: remove the Revolut-created bill -->
    <record id="action_server_remove_attached_bill" model="ir.actions.server">
        <field name="name">Remove attached Bill</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_remove_attached_bill()</field>
    </record>

    <!-- ── Server action: post bill + reconcile bank line ↔ bill -->
    <record id="action_server_reconcile_vendor_bills" model="ir.actions.server">
        <field name="name">3. Reconcile injected bill &amp; tx</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_reconcile_vendor_bill()</field>
    </record>

    <!-- ── Server action: unreconcile bank line ↔ bill (keep link) -->
    <record id="action_server_unreconcile_bill" model="ir.actions.server">
        <field name="name">Unreconcile injected bill &amp; tx</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_unreconcile_bill()</field>
    </record>

    <!-- ── Server action: download attached/uploaded/bill documents -->
    <record id="action_server_download_documents" model="ir.actions.server">
        <field name="name">Download Documents</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_download_documents()</field>
    </record>

    <!-- ── Server action: export bills for the accountant (year/month/partner/tx.pdf) -->
    <record id="action_server_export_bills_for_accountant" model="ir.actions.server">
        <field name="name">Export Bills for Accountant</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_export_bills_for_accountant()</field>
    </record>

    <!-- ── Server action: backfill the Data Source analytic onto linked bills -->
    <record id="action_server_apply_data_source_analytic" model="ir.actions.server">
        <field name="name">Apply Data Source analytic</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_apply_data_source_analytic()</field>
    </record>

    <!-- ── Server action: re-evaluate & merge analytic rules onto linked docs -->
    <record id="action_server_reapply_analytic_rules" model="ir.actions.server">
        <field name="name">Reapply analytic rules</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_reapply_analytic_rules()</field>
    </record>

    <!-- ── Server action: reconcile non-bill lines (transfers + fees) -->
    <record id="action_server_reconcile_transfers_fees" model="ir.actions.server">
        <field name="name">Reconcile transfers &amp; fees</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_reconcile_transfers_fees()</field>
    </record>

    <!-- ── Server action: diagnose a wrong bank journal dashboard balance -->
    <record id="action_server_diagnose_journal_balance" model="ir.actions.server">
        <field name="name">Diagnose bank journal balance</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_diagnose_journal_balance()</field>
    </record>

    <!-- ── Server action: clean up orphan/duplicate Revolut bank lines -->
    <record id="action_server_cleanup_orphan_statement_lines" model="ir.actions.server">
        <field name="name">Clean up orphan bank lines</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_cleanup_orphan_statement_lines()</field>
    </record>

    <!-- ── Server action: full teardown — remove EVERYTHING from accounting -->
    <record id="action_server_remove_all_from_accounting" model="ir.actions.server">
        <field name="name">Remove from Accounting (full)</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_remove_all_from_accounting()</field>
    </record>

    <!-- ── Server action: batch unlink vendor bill (appears in ⚙ Actions menu) -->
    <record id="action_server_unlink_vendor_bills" model="ir.actions.server">
        <field name="name">Unlink Vendor Bill</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_unlink_vendor_bill()</field>
    </record>

    <!-- ── Server action: batch remove attachments (appears in ⚙ Actions menu) -->
    <record id="action_server_remove_attachments" model="ir.actions.server">
        <field name="name">Remove Attachments</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_remove_attachments()</field>
    </record>

    <!-- ── Server action: Gmail auto-attach single match (appears in ⚙ Actions menu) -->
    <record id="action_server_gmail_auto_attach_single" model="ir.actions.server">
        <field name="name">Gmail: Auto-Attach Single Match</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_gmail_auto_attach_single()</field>
    </record>

    <!-- ── Server action: Gmail AI-pick best attachment (appears in ⚙ Actions menu) -->
    <record id="action_server_gmail_ai_pick" model="ir.actions.server">
        <field name="name">Gmail: AI-Pick Best Attachment</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_gmail_ai_pick()</field>
    </record>

    <!-- ── Server action: batch delete transactions (appears in ⚙ Actions menu) -->
    <record id="action_server_delete_transactions" model="ir.actions.server">
        <field name="name">Delete Transactions</field>
        <field name="model_id" ref="model_revolut_transaction"/>
        <field name="binding_model_id" ref="model_revolut_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_delete_transactions()</field>
    </record>

    <!-- ── Window action ────────────────────────────────────────────────────── -->
    <record id="action_revolut_helper_transactions" model="ir.actions.act_window">
        <field name="name">Revolut Transactions</field>
        <field name="res_model">revolut.transaction</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_revolut_transaction_search"/>
        <field name="context">{'search_default_state_completed': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No transactions yet</p>
            <p>Click <strong>Sync All from Revolut</strong> to download all transactions.</p>
        </field>
    </record>

    <!-- ── Window action: Income View (amount > 0, hard domain) ─────────────── -->
    <record id="action_revolut_income_view" model="ir.actions.act_window">
        <field name="name">Income Transactions</field>
        <field name="res_model">revolut.transaction</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_revolut_transaction_search"/>
        <field name="domain">[('amount', '&gt;', 0)]</field>
        <field name="context">{'search_default_state_completed': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No income transactions found</p>
            <p>Income transactions (positive amounts) will appear here once synced.</p>
        </field>
    </record>

    <!-- ── Extend Journal Items tree: optional Revolut TX ID column ─────── -->
    <record id="view_move_line_tree_revolut_tx_ref" model="ir.ui.view">
        <field name="name">account.move.line.tree.revolut.tx.ref</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='matching_number']" position="after">
                <field name="revolut_tx_ref" optional="hide"/>
            </xpath>
        </field>
    </record>

</odoo>
