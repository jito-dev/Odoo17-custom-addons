# Odoo Core requirements
# odoo==17.0

# --- AI Addons Dependencies ---
# Please uncomment the lines for the addons you plan to use.

# For hr_recruitment_openai
openai==2.6.1

# For hr_recruitment_gemini
# google-generativeai==0.8.5

# For jito_document_template
docxtpl
# System dependency for PDF generation (install via apt):
#   sudo apt-get install -y libreoffice-writer --no-install-recommends

# For upwork_simple_accounting_integration
# Required for the SOCKS5 proxy and Chrome TLS fingerprint Upwork's Cloudflare
# demands; urllib cannot speak SOCKS5, so without it the integration silently
# loses its proxy and starts getting IP-blocked.
curl_cffi
