<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================================ -->
    <!-- Transaction — Tree View                                      -->
    <!-- ============================================================ -->
    <record id="view_usa_transaction_tree" model="ir.ui.view">
        <field name="name">usa.transaction.tree</field>
        <field name="model">usa.transaction</field>
        <field name="arch" type="xml">
            <tree string="Upwork Transactions"
                  create="0"
                  decoration-success="payment_status == 'SUCCESS'"
                  decoration-muted="not transaction_creation_date">
                <field name="record_id" string="Transaction ID"/>
                <field name="transaction_review_due_date" string="Date"/>
                <field name="accounting_subtype" string="Subtype"/>
                <field name="description_ui" string="Description"/>
                <field name="transaction_amount_raw" string="Amount"/>
                <field name="transaction_amount_currency" string="Currency"/>
                <field name="assignment_company_name" string="Client"/>
                <field name="payment_status" string="Payment Status"
                       widget="badge"
                       decoration-success="payment_status == 'SUCCESS'"
                       decoration-warning="payment_status and payment_status != 'SUCCESS'"/>
                <field name="invoice_state" string="Invoice"
                       widget="badge"
                       decoration-warning="invoice_state == 'draft'"
                       decoration-success="invoice_state == 'generated'"/>
                <field name="has_upwork_invoice" string="Attached File" optional="show" readonly="1"/>
                <field name="extraction_state" string="AI Extraction" optional="show"
                       widget="badge"
                       decoration-info="extraction_state == 'pending'"
                       decoration-warning="extraction_state == 'processing'"
                       decoration-success="extraction_state == 'done'"
                       decoration-danger="extraction_state == 'failed'"
                       decoration-muted="extraction_state == 'idle'"/>
                <field name="transaction_type" string="Type" optional="hide"/>
                <field name="amount_credited_raw" string="Credited" optional="hide"/>
                <field name="running_balance_raw" string="Running Balance" optional="hide"/>
                <field name="assignment_developer_name" string="Freelancer" optional="hide"/>
                <field name="parsed_contract_id" string="Contract ID" optional="hide"/>
                <field name="parsed_hours" string="Hours" optional="hide"/>
                <field name="parsed_rate" string="Rate ($/hr)" optional="hide"/>
                <field name="parsed_period_start" string="Period Start" optional="hide"/>
                <field name="parsed_period_end" string="Period End" optional="hide"/>
                <field name="transaction_creation_date" string="Creation Date" optional="hide"/>
                <field name="fully_paid_date" string="Fully Paid Date" optional="hide"/>
                <field name="is_injected" string="Injected" optional="show"
                       decoration-success="is_injected"
                       decoration-muted="not is_injected"/>
                <field name="mapped_account_display" string="GL Account" optional="hide"/>
                <field name="injection_mode" string="Posting Mode" optional="hide"/>
                <field name="move_id" string="Journal Entry" optional="hide"/>
                <field name="customer_invoice_id" string="Customer Invoice" optional="hide"/>
                <field name="customer_refund_id" string="Customer Credit Note" optional="hide"/>
                <field name="vendor_refund_id" string="Vendor Credit Note" optional="hide"/>
                <field name="has_vendor_bill" string="Bill" widget="boolean"
                       optional="hide" readonly="1"
                       decoration-success="has_vendor_bill"
                       decoration-muted="not has_vendor_bill"/>
                <field name="is_bill_reconciled" string="Reconciled" widget="boolean"
                       optional="hide" readonly="1"
                       decoration-success="is_bill_reconciled"
                       decoration-muted="not is_bill_reconciled"/>
            </tree>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Transaction — Form View                                      -->
    <!-- ============================================================ -->
    <record id="view_usa_transaction_form" model="ir.ui.view">
        <field name="name">usa.transaction.form</field>
        <field name="model">usa.transaction</field>
        <field name="arch" type="xml">
            <form string="Upwork Transaction" create="0">
                <header>
                    <!-- Accounting injection -->
                    <button name="action_inject_to_accounting"
                            type="object"
                            string="Inject to Accounting"
                            class="btn-primary"
                            icon="fa-arrow-right"
                            invisible="is_injected"/>
                    <button name="action_remove_from_accounting"
                            type="object"
                            string="Remove from Accounting"
                            class="btn-secondary"
                            icon="fa-undo"
                            invisible="not is_injected"/>

                    <!-- Customer invoice creation (revenue, document mode) -->
                    <button name="action_create_customer_invoice"
                            type="object"
                            string="Create Customer Invoice &amp; Reconcile"
                            class="btn-primary"
                            icon="fa-magic"
                            invisible="has_customer_invoice or not has_upwork_invoice or injection_mode != 'customer_invoice'"/>
                    <button name="action_create_draft_customer_invoice"
                            type="object"
                            string="Create Draft Invoice"
                            class="btn-secondary"
                            icon="fa-file-text-o"
                            invisible="has_customer_invoice or not has_upwork_invoice or injection_mode != 'customer_invoice'"/>
                    <button name="action_reconcile_customer_invoice"
                            type="object"
                            string="Reconcile Invoice"
                            class="btn-primary"
                            icon="fa-check-square-o"
                            invisible="not has_customer_invoice or not is_injected or is_customer_invoice_reconciled"/>
                    <button name="action_open_customer_invoice"
                            type="object"
                            string="Open Invoice"
                            class="btn-link"
                            icon="fa-external-link"
                            invisible="not has_customer_invoice"/>

                    <!-- Customer credit note (refund to client) -->
                    <button name="action_create_customer_refund"
                            type="object"
                            string="Create Customer Credit Note &amp; Reconcile"
                            class="btn-primary"
                            icon="fa-magic"
                            invisible="has_customer_refund or not has_upwork_invoice or injection_mode != 'customer_refund'"/>
                    <button name="action_open_customer_refund"
                            type="object"
                            string="Open Credit Note"
                            class="btn-link"
                            icon="fa-external-link"
                            invisible="not has_customer_refund"/>

                    <!-- Vendor bill creation (fees/charges, document mode) -->
                    <button name="action_create_vendor_bill"
                            type="object"
                            string="Create Bill &amp; Reconcile"
                            class="btn-primary"
                            icon="fa-magic"
                            invisible="has_vendor_bill or not has_upwork_invoice or injection_mode != 'vendor_bill'"/>
                    <button name="action_create_draft_vendor_bill"
                            type="object"
                            string="Create Draft Bill"
                            class="btn-secondary"
                            icon="fa-file-text-o"
                            invisible="has_vendor_bill or not has_upwork_invoice or injection_mode != 'vendor_bill'"/>
                    <!-- Reconciliation & navigation -->
                    <button name="action_reconcile_vendor_bill"
                            type="object"
                            string="Reconcile Bill"
                            class="btn-primary"
                            icon="fa-check-square-o"
                            invisible="not has_vendor_bill or not is_injected or is_bill_reconciled"/>
                    <button name="action_open_vendor_bill"
                            type="object"
                            string="Open Bill"
                            class="btn-link"
                            icon="fa-external-link"
                            invisible="not has_vendor_bill"/>

                    <!-- Vendor credit note (Upwork fee return) -->
                    <button name="action_create_vendor_refund"
                            type="object"
                            string="Create Vendor Credit Note &amp; Reconcile"
                            class="btn-primary"
                            icon="fa-magic"
                            invisible="has_vendor_refund or not has_upwork_invoice or injection_mode != 'vendor_refund'"/>
                    <button name="action_open_vendor_refund"
                            type="object"
                            string="Open Vendor Credit Note"
                            class="btn-link"
                            icon="fa-external-link"
                            invisible="not has_vendor_refund"/>

                    <!-- Contract details fetch (only for hourly billing without detail yet) -->
                    <button name="action_request_contract_details"
                            type="object"
                            string="Request Contract Details"
                            class="btn btn-secondary"
                            invisible="not parsed_contract_id or contract_detail_id"/>

                    <!-- PDF extraction (only when PDF attached and not yet extracted) -->
                    <button name="action_extract_data_from_invoice"
                            type="object"
                            string="Extract Invoice Data"
                            class="btn btn-secondary"
                            invisible="not upwork_invoice_pdf or extracted_client_address"/>

                    <!-- Invoice generation -->
                    <button name="action_generate_invoice"
                            type="object"
                            string="Generate Invoice"
                            class="oe_highlight"
                            invisible="invoice_state == 'generated'"/>
                    <button name="action_reset_invoice"
                            type="object"
                            string="Reset Invoice"
                            class="btn btn-outline-danger btn-sm"
                            invisible="invoice_state == 'draft'"
                            confirm="Delete generated invoice files and reset to draft?"/>

                    <field name="invoice_state" widget="statusbar"
                           statusbar_visible="draft,generated" readonly="1"/>
                </header>

                <sheet>
                    <!-- Hidden modifier fields -->
                    <field name="is_injected" invisible="1"/>
                    <field name="move_id" invisible="1"/>
                    <field name="generated_docx_id" invisible="1"/>
                    <field name="generated_pdf_id" invisible="1"/>
                    <field name="has_upwork_invoice" invisible="1"/>
                    <field name="has_vendor_bill" invisible="1"/>
                    <field name="is_bill_reconciled" invisible="1"/>
                    <field name="injection_mode" invisible="1"/>
                    <field name="has_customer_invoice" invisible="1"/>
                    <field name="is_customer_invoice_reconciled" invisible="1"/>
                    <field name="has_customer_refund" invisible="1"/>
                    <field name="has_vendor_refund" invisible="1"/>
                    <field name="is_refund_reconciled" invisible="1"/>

                    <!-- Stat buttons -->
                    <div name="button_box" class="oe_button_box">
                        <!-- Journal Entry smart button -->
                        <button name="action_view_journal_entry"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-book"
                                invisible="not move_id">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Journal Entry</span>
                            </div>
                        </button>

                        <!-- Customer Invoice smart button -->
                        <button name="action_open_customer_invoice"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-file-text-o"
                                invisible="not has_customer_invoice">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Customer Invoice</span>
                            </div>
                        </button>

                        <!-- Client (Customer) partner smart button -->
                        <button name="action_open_client_partner"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-user"
                                invisible="not has_customer_invoice">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Customer</span>
                            </div>
                        </button>

                        <!-- Vendor Bill smart button -->
                        <button name="action_open_vendor_bill"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-file-text"
                                invisible="not has_vendor_bill">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Vendor Bill</span>
                            </div>
                        </button>

                        <!-- Customer Credit Note smart button -->
                        <button name="action_open_customer_refund"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-rotate-left"
                                invisible="not has_customer_refund">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Customer Credit Note</span>
                            </div>
                        </button>

                        <!-- Vendor Credit Note smart button -->
                        <button name="action_open_vendor_refund"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-rotate-left"
                                invisible="not has_vendor_refund">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Vendor Credit Note</span>
                            </div>
                        </button>

                        <!-- Contract Detail smart button -->
                        <button name="action_request_contract_details"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-file-text-o"
                                invisible="not contract_detail_id">
                            <field name="contract_detail_id" widget="statinfo"
                                   string="Contract Detail" options="{'no_open': False}"/>
                        </button>

                        <!-- Invoice PDF indicator -->
                        <button type="object"
                                name="action_download_upwork_invoice"
                                class="oe_stat_button"
                                icon="fa-paperclip"
                                invisible="not has_upwork_invoice">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Invoice PDF</span>
                            </div>
                        </button>
                    </div>

                    <!-- Ribbon for Generated state -->
                    <widget name="web_ribbon" title="Invoice Generated"
                            bg_color="text-bg-success"
                            invisible="invoice_state != 'generated'"/>

                    <div class="oe_title">
                        <h1>
                            <field name="description_ui" readonly="1"
                                   placeholder="Transaction Description"/>
                        </h1>
                        <div class="d-flex align-items-center gap-3 mt-1">
                            <span class="badge text-bg-secondary"
                                  invisible="not transaction_type">
                                <field name="transaction_type" readonly="1" nolabel="1"/>
                            </span>
                            <span class="badge text-bg-info"
                                  invisible="not accounting_subtype">
                                <field name="accounting_subtype" readonly="1" nolabel="1"/>
                            </span>
                        </div>
                    </div>

                    <!-- Transaction Info -->
                    <group>
                        <group string="Transaction Info">
                            <field name="record_id" readonly="1"/>
                            <field name="row_number" readonly="1"/>
                            <field name="transaction_type" readonly="1"/>
                            <field name="accounting_subtype" readonly="1"/>
                            <field name="payment_status" readonly="1"/>
                            <field name="payment_guaranteed" readonly="1"/>
                            <field name="prefix" readonly="1"
                                   invisible="not prefix"/>
                            <field name="vendor_bill_id" string="Vendor Bill"
                                   readonly="1" invisible="not has_vendor_bill"/>
                            <field name="customer_invoice_id" string="Customer Invoice"
                                   readonly="1" invisible="not has_customer_invoice"/>
                            <field name="customer_refund_id" string="Customer Credit Note"
                                   readonly="1" invisible="not has_customer_refund"/>
                            <field name="vendor_refund_id" string="Vendor Credit Note"
                                   readonly="1" invisible="not has_vendor_refund"/>
                            <field name="move_id" string="Journal Entry"
                                   readonly="1" invisible="not is_injected"/>
                            <field name="statement_line_id" string="Bank Statement Line"
                                   readonly="1" invisible="not is_injected"/>
                            <field name="mapped_account_display" string="Mapped GL Account"
                                   readonly="1" invisible="not is_injected"/>
                            <field name="injection_mode" string="Posting Mode" readonly="1"/>
                        </group>
                        <group string="Dates">
                            <field name="transaction_creation_date" readonly="1"/>
                            <field name="transaction_review_due_date" readonly="1"
                                   invisible="not transaction_review_due_date"/>
                            <field name="fully_paid_date" readonly="1"
                                   invisible="not fully_paid_date"/>
                            <field name="sync_date" readonly="1" string="Last Synced"/>
                        </group>
                    </group>

                    <!-- Amounts -->
                    <separator string="Amounts"/>
                    <group>
                        <group string="Transaction">
                            <field name="transaction_amount_raw" readonly="1" string="Transaction Amount"/>
                            <field name="transaction_amount_currency" readonly="1" string="Currency"/>
                            <field name="amount_credited_raw" readonly="1" string="Credited to User"/>
                            <field name="amount_credited_currency" readonly="1" string="Credited Currency"/>
                        </group>
                        <group string="Balance &amp; Other">
                            <field name="running_balance_raw" readonly="1" string="Running Balance"/>
                            <field name="running_balance_currency" readonly="1" string="Balance Currency"/>
                            <field name="amount_sent_orig_raw" readonly="1" string="Amount Sent (Orig)"
                                   invisible="not amount_sent_orig_raw"/>
                            <field name="amount_sent_orig_currency" readonly="1" string="Sent Currency"
                                   invisible="not amount_sent_orig_raw"/>
                            <field name="payment_raw" readonly="1" string="Payment"
                                   invisible="not payment_raw"/>
                            <field name="payment_currency" readonly="1" string="Payment Currency"
                                   invisible="not payment_raw"/>
                            <field name="remainder" readonly="1" string="Remainder"
                                   invisible="not remainder"/>
                        </group>
                    </group>

                    <!-- Description -->
                    <separator string="Description"/>
                    <group>
                        <field name="description_ui" readonly="1" string="Description (UI)"/>
                        <field name="purchase_order_number" readonly="1"
                               invisible="not purchase_order_number"/>
                    </group>
                    <field name="description" readonly="1" string="Full Description"
                           invisible="not description"/>

                    <!-- Parsed contract details (hourly billing transactions) -->
                    <group string="Contract Details" invisible="not parsed_contract_id">
                        <group>
                            <field name="parsed_contract_id" readonly="1" string="Contract ID"/>
                            <field name="parsed_hours" readonly="1" string="Hours"/>
                            <field name="parsed_rate" readonly="1" string="Rate ($/hr)"/>
                        </group>
                        <group>
                            <field name="parsed_period_start" readonly="1" string="Period Start"/>
                            <field name="parsed_period_end" readonly="1" string="Period End"/>
                        </group>
                        <div invisible="not contract_detail_id">
                            <field name="contract_detail_id" readonly="1" nolabel="1"
                                   options="{'no_create': True}"
                                   invisible="not contract_detail_id"/>
                        </div>
                    </group>

                    <!-- Relations -->
                    <separator string="Related Records"
                               invisible="not related_transaction_id and not related_invoice_id and not related_assignment and not related_accounting_entity"/>
                    <group invisible="not related_transaction_id and not related_invoice_id and not related_assignment and not related_accounting_entity">
                        <group>
                            <field name="related_transaction_id" readonly="1"
                                   invisible="not related_transaction_id"/>
                            <field name="related_invoice_id" readonly="1"
                                   invisible="not related_invoice_id"/>
                            <field name="related_assignment" readonly="1"
                                   invisible="not related_assignment"/>
                            <field name="related_accounting_entity" readonly="1"
                                   invisible="not related_accounting_entity"/>
                        </group>
                        <group>
                            <field name="related_user_payment_method" readonly="1"
                                   invisible="not related_user_payment_method"/>
                            <field name="fixed_price_earmark" readonly="1"
                                   invisible="not fixed_price_earmark"/>
                        </group>
                    </group>

                    <!-- Assignment Info -->
                    <separator string="Assignment / Contract"
                               invisible="not assignment_company_name and not assignment_developer_name and not assignment_agency_name and not upwork_invoice_pdf"/>
                    <group invisible="not assignment_company_name and not assignment_developer_name and not assignment_agency_name">
                        <group string="Parties">
                            <field name="assignment_agency_name" readonly="1"
                                   invisible="not assignment_agency_name"/>
                            <field name="assignment_company_name" readonly="1"
                                   invisible="not assignment_company_name"/>
                            <field name="assignment_developer_name" readonly="1"
                                   invisible="not assignment_developer_name"/>
                        </group>
                        <group string="Team IDs" invisible="not assignment_team_id">
                            <field name="assignment_team_id" readonly="1"
                                   invisible="not assignment_team_id"/>
                            <field name="assignment_team_reference" readonly="1"
                                   invisible="not assignment_team_reference"/>
                            <field name="assignment_team_company_id" readonly="1"
                                   invisible="not assignment_team_company_id"/>
                            <field name="assignment_team_company_reference" readonly="1"
                                   invisible="not assignment_team_company_reference"/>
                            <field name="assignment_team_user_id" readonly="1"
                                   invisible="not assignment_team_user_id"/>
                            <field name="assignment_team_user_reference" readonly="1"
                                   invisible="not assignment_team_user_reference"/>
                        </group>
                    </group>

                    <!-- Upwork Invoice File -->
                    <separator string="Upwork Invoice File"/>
                    <div>
                        <!-- When file is attached: show filename, buttons, and preview -->
                        <div invisible="not upwork_invoice_pdf">
                            <group>
                                <group>
                                    <field name="upwork_invoice_filename" string="Filename" readonly="1"/>
                                </group>
                            </group>
                            <div class="d-flex align-items-center gap-2 mb-2">
                                <button name="action_download_upwork_invoice" type="object"
                                        string="Download" class="btn btn-outline-primary btn-sm"/>
                                <button name="action_remove_upwork_invoice" type="object"
                                        string="Remove File" class="btn btn-outline-danger btn-sm"
                                        confirm="Remove the attached invoice file?"/>
                                <button name="action_extract_data_from_invoice" type="object"
                                        string="Extract Data from Attached File"
                                        class="btn btn-outline-secondary btn-sm"/>
                            </div>
                            <!-- Small inline PDF preview -->
                            <div style="height: 300px; overflow: hidden; border: 1px solid #dee2e6; border-radius: 4px;">
                                <field name="upwork_invoice_pdf"
                                       widget="pdf_viewer"
                                       filename="upwork_invoice_filename"
                                       nolabel="1"
                                       readonly="1"/>
                            </div>
                            <!-- ── Extracted Data ── -->
                            <separator string="Extracted Data"/>
                            <div class="d-flex align-items-center gap-2 mb-2"
                                 invisible="extraction_state == 'idle'">
                                <field name="extraction_state"
                                       widget="badge"
                                       decoration-info="extraction_state == 'pending'"
                                       decoration-warning="extraction_state == 'processing'"
                                       decoration-success="extraction_state == 'done'"
                                       decoration-danger="extraction_state == 'failed'"
                                       nolabel="1" readonly="1"/>
                                <span class="text-muted small"
                                      invisible="not extraction_status">
                                    <field name="extraction_status" nolabel="1" readonly="1"/>
                                </span>
                            </div>
                            <group>
                                <group>
                                    <field name="extracted_client_street" string="Street"/>
                                    <field name="extracted_client_city" string="City"/>
                                    <field name="extracted_client_state" string="State"/>
                                    <field name="extracted_client_zip" string="ZIP"/>
                                    <field name="extracted_client_country" string="Country"/>
                                    <field name="extracted_client_address" string="Full Address (raw)"/>
                                </group>
                            </group>
                            <div class="mt-1">
                                <button name="action_force_address_update"
                                        type="object"
                                        string="Force Address Update"
                                        class="btn btn-warning btn-sm"
                                        icon="fa-refresh"
                                        invisible="not upwork_invoice_pdf"
                                        confirm="Overwrite the client partner's address (street, city, state, zip, country) with the extracted data?"/>
                            </div>
                        </div>
                        <!-- When no file: info message -->
                        <div invisible="upwork_invoice_pdf" class="alert alert-info" role="alert">
                            No invoice PDF attached yet. Use
                            <strong>Upload Upwork Invoices</strong>
                            from the menu to auto-match and attach invoice files.
                        </div>
                    </div>

                    <!-- Tabs: Raw JSON + Invoice Generation -->
                    <notebook>
                        <!-- ── Raw JSON ── -->
                        <page string="Raw JSON" name="raw_json">
                            <div class="alert alert-secondary mb-2" role="alert">
                                <small>Original API response for this transaction row.</small>
                            </div>
                            <field name="raw_json"
                                   nolabel="1"
                                   widget="ace"
                                   options="{'mode': 'json'}"
                                   readonly="1"/>
                        </page>

                        <!-- ── Invoice Generation ── -->
                        <page string="Invoice Generation" name="invoice_generation">
                            <group>
                                <group>
                                    <field name="invoice_number" string="Invoice Number"
                                           placeholder="e.g. INV-2026-001"/>
                                </group>
                            </group>
                            <div class="d-flex align-items-center gap-2 mb-3">
                                <button name="action_build_invoice_context"
                                        type="object"
                                        string="Rebuild Context"
                                        class="btn btn-secondary"/>
                            </div>

                            <!-- Generated Documents -->
                            <separator string="Generated Documents"/>
                            <group>
                                <group string="Files">
                                    <label for="generated_docx_id" string="DOCX"/>
                                    <div class="d-flex align-items-center gap-2">
                                        <field name="generated_docx_id" readonly="1" nolabel="1"
                                               options="{'no_create': True, 'no_open': False}"/>
                                        <button name="action_download_docx" type="object"
                                                string="Download"
                                                class="btn btn-outline-secondary btn-sm"
                                                invisible="not generated_docx_id"/>
                                    </div>
                                    <label for="generated_pdf_id" string="PDF"/>
                                    <div class="d-flex align-items-center gap-2">
                                        <field name="generated_pdf_id" readonly="1" nolabel="1"
                                               options="{'no_create': True, 'no_open': False}"/>
                                        <button name="action_download_pdf" type="object"
                                                string="Download"
                                                class="btn btn-outline-secondary btn-sm"
                                                invisible="not generated_pdf_id"/>
                                    </div>
                                </group>
                            </group>

                            <!-- Template Variables with resolved values -->
                            <div invisible="not rendered_vars_html">
                                <separator string="Template Variables"/>
                                <field name="rendered_vars_html" nolabel="1" readonly="1"/>
                            </div>

                            <!-- Context Preview -->
                            <div invisible="not context_data">
                                <separator string="Built Context (JSON)"/>
                                <field name="context_data"
                                       nolabel="1"
                                       widget="ace"
                                       options="{'mode': 'json'}"
                                       readonly="1"/>
                            </div>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Transaction — Search View                                    -->
    <!-- ============================================================ -->
    <record id="view_usa_transaction_search" model="ir.ui.view">
        <field name="name">usa.transaction.search</field>
        <field name="model">usa.transaction</field>
        <field name="arch" type="xml">
            <search string="Upwork Transactions">
                <field name="description_ui" string="Description"/>
                <field name="record_id" string="Record ID"/>
                <field name="assignment_company_name" string="Client"/>
                <field name="assignment_developer_name" string="Freelancer"/>
                <field name="accounting_subtype" string="Subtype"/>
                <separator/>
                <filter string="Invoice Draft" name="filter_invoice_draft"
                        domain="[('invoice_state', '=', 'draft')]"/>
                <filter string="Invoice Generated" name="filter_invoice_generated"
                        domain="[('invoice_state', '=', 'generated')]"/>
                <separator/>
                <filter string="This Month" name="filter_this_month"
                        domain="[('transaction_review_due_date', '&gt;=', (context_today() + relativedelta(day=1)).strftime('%Y-%m-%d')),
                                 ('transaction_review_due_date', '&lt;', (context_today() + relativedelta(months=1, day=1)).strftime('%Y-%m-%d'))]"/>
                <filter string="Date" name="filter_date" date="transaction_review_due_date"/>
                <filter string="Creation Date" name="filter_creation_date" date="transaction_creation_date"/>
                <filter string="Fully Paid Date" name="filter_fully_paid_date" date="fully_paid_date"/>
                <separator/>
                <filter string="Has Invoice PDF" name="filter_has_pdf"
                        domain="[('upwork_invoice_pdf', '!=', False)]"/>
                <filter string="Has Contract Detail" name="filter_has_contract"
                        domain="[('contract_detail_id', '!=', False)]"/>
                <filter string="No Contract Detail" name="filter_no_contract"
                        domain="[('parsed_contract_id', '!=', False), ('contract_detail_id', '=', False)]"/>
                <separator/>
                <filter string="Injected to Accounting" name="filter_injected"
                        domain="[('is_injected', '=', True)]"/>
                <filter string="Not Injected" name="filter_not_injected"
                        domain="[('is_injected', '=', False)]"/>
                <separator/>
                <filter string="Reconciled" name="filter_reconciled"
                        domain="[('is_bill_reconciled', '=', True)]"/>
                <filter string="Not Reconciled" name="filter_not_reconciled"
                        domain="[('is_bill_reconciled', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Type" name="group_by_type"
                            context="{'group_by': 'transaction_type'}"/>
                    <filter string="Subtype" name="group_by_subtype"
                            context="{'group_by': 'accounting_subtype'}"/>
                    <filter string="Client" name="group_by_company"
                            context="{'group_by': 'assignment_company_name'}"/>
                    <filter string="Payment Status" name="group_by_payment_status"
                            context="{'group_by': 'payment_status'}"/>
                    <filter string="Invoice Status" name="group_by_invoice_state"
                            context="{'group_by': 'invoice_state'}"/>
                    <filter string="Date" name="group_by_date"
                            context="{'group_by': 'transaction_review_due_date:month'}"/>
                    <filter string="Creation Date" name="group_by_creation_date"
                            context="{'group_by': 'transaction_creation_date:month'}"/>
                    <filter string="Fully Paid Date" name="group_by_fully_paid_date"
                            context="{'group_by': 'fully_paid_date:month'}"/>
                    <filter string="Reconciled" name="group_by_reconciled"
                            context="{'group_by': 'is_bill_reconciled'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Transaction — Action                                         -->
    <!-- ============================================================ -->
    <record id="action_usa_transaction" model="ir.actions.act_window">
        <field name="name">Upwork Transactions</field>
        <field name="res_model">usa.transaction</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No transactions yet</p>
            <p>Use the <strong>Sync</strong> button on the Accounting Ledger tab to download transactions from Upwork.</p>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Batch Actions — appear in Action dropdown of list view       -->
    <!-- ============================================================ -->
    <record id="action_server_batch_request_contract_details" model="ir.actions.server">
        <field name="name">Request Contract Details</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_batch_request_contract_details()</field>
    </record>

    <record id="action_server_batch_extract_data" model="ir.actions.server">
        <field name="name">Extract Data from Attached File</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_batch_extract_data_from_invoice()</field>
    </record>

    <record id="action_server_batch_enrich_clients" model="ir.actions.server">
        <field name="name">Enrich Client Addresses</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_enrich_client_addresses()</field>
    </record>

    <record id="action_server_batch_generate_invoice" model="ir.actions.server">
        <field name="name">Generate Custom Invoice</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_batch_generate_invoice()</field>
    </record>

    <record id="action_server_batch_remove_upwork_invoice" model="ir.actions.server">
        <field name="name">Remove Attached File</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_batch_remove_upwork_invoice()</field>
    </record>

    <!-- ============================================================ -->
    <!-- Accounting Injection — Batch Actions                         -->
    <!-- ============================================================ -->
    <record id="action_server_batch_inject_to_accounting" model="ir.actions.server">
        <field name="name">Inject to Accounting</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_inject_to_accounting()</field>
    </record>

    <record id="action_server_batch_remove_from_accounting" model="ir.actions.server">
        <field name="name">Remove from Accounting</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_remove_from_accounting()</field>
    </record>

    <!-- ============================================================ -->
    <!-- Vendor Bill — Batch Actions                                  -->
    <!-- ============================================================ -->
    <record id="action_server_batch_create_vendor_bills" model="ir.actions.server">
        <field name="name">Create Bills &amp; Reconcile</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_create_vendor_bill()</field>
    </record>

    <record id="action_server_batch_create_draft_vendor_bills" model="ir.actions.server">
        <field name="name">Create Draft Bills</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_create_draft_vendor_bill()</field>
    </record>

    <record id="action_server_batch_reconcile_vendor_bills" model="ir.actions.server">
        <field name="name">Reconcile Bills</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_reconcile_vendor_bill()</field>
    </record>

    <record id="action_server_batch_fix_bill_vendor" model="ir.actions.server">
        <field name="name">Fix Bill Vendor (→ Upwork)</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_fix_bill_vendor()</field>
    </record>

    <!-- ============================================================ -->
    <!-- One-click: create ALL documents (by mode) & reconcile        -->
    <!-- ============================================================ -->
    <record id="action_server_batch_create_documents" model="ir.actions.server">
        <field name="name">Create Accounting Documents &amp; Reconcile</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_create_accounting_documents()</field>
    </record>

    <!-- ============================================================ -->
    <!-- One-click END-TO-END: inject + create docs + reconcile       -->
    <!-- ============================================================ -->
    <record id="action_server_batch_inject_create_documents" model="ir.actions.server">
        <field name="name">Inject &amp; Create Docs &amp; Reconcile</field>
        <field name="model_id" ref="model_usa_transaction"/>
        <field name="binding_model_id" ref="model_usa_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_inject_and_create_documents()</field>
    </record>

</odoo>
